import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.example.ijstestapp',
  appName: 'ijstestapp',
  webDir: 'dist',
  bundledWebRuntime: false
};

export default config;
